query FetchMossallAdmin($adminId: String!) {
  fetchMossallAdmin(adminId: $adminId) {
    id
    firstName
    lastName
    email
    phoneNumber
    uniqueIdentifier
    address
    position
    blocked
    createdAt
  }
}