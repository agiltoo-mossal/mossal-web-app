mutation CreateBulkPaymentOrder($inputs: [BulkPaymentInput!]!) {
  createBulkPaymentOrder(inputs: $inputs) {
    id
    number
    firstName
    lastName
    phoneNumber
    amount
    status
    reason
    wallet
    organization
    createdBy
    createdAt
  }
}
